% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report}
\alias{report}
\title{What a source's metadata is missing}
\usage{
report(x)
}
\arguments{
\item{x}{A source from \code{\link[=src]{src()}}.}
}
\value{
A tibble with columns \code{check} (a short id), \code{severity}
(\code{"blocks"}, \code{"degrades"} or \code{"note"}), \code{what}, \code{consequence},
\code{fix_kind} (\code{"open_option"}, \code{"vrt"}, \code{"gdal_cli"} or \code{"r_call"}) and
\code{fix}. Zero rows when the source says everything it should.
}
\description{
\code{report()} returns one row per finding: what the source does not say about
itself, what that will cost the read, and the exact text that would fix it.
}
\details{
Nothing in this package ever applies a fix. The \code{fix} column is text for
you to run or to pass back in, and the only ways to act on one are to hand
an open option to \code{\link[=src]{src()}}, to run the suggested command yourself, or to
call a verb named for what it does, such as \code{\link[=warp]{warp()}}. A package that
quietly wrapped a georeference-less file in a VRT that supplies a
geotransform would be hiding exactly the thing you need to know.

The findings are computed once when the source is opened, from calls GDAL
has already cached, so \code{report()} reads nothing and costs nothing.
}
\examples{
x <- src(system.file("extdata/test.tif", package = "GDAL7"))
report(x)
}
