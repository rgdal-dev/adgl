% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extent.R
\name{extent_to_bbox}
\alias{extent_to_bbox}
\alias{bbox_to_extent}
\title{Extent order}
\usage{
extent_to_bbox(extent)

bbox_to_extent(bbox)
}
\arguments{
\item{extent}{An extent, \code{c(xmin, xmax, ymin, ymax)}.}

\item{bbox}{A bounding box, \code{c(xmin, ymin, xmax, ymax)}.}
}
\value{
The same four numbers in the other order, named.
}
\description{
\code{adgl} takes and returns an extent as \code{c(xmin, xmax, ymin, ymax)}, which is
what \code{vaster}, \code{ximage} and \code{graphics::par("usr")} use. \code{wk::rct()} and
GDAL's own bounding-box arguments use \code{c(xmin, ymin, xmax, ymax)}. These
two functions convert between them, and they are exported because a caller
mixing \code{adgl} with \code{wk} needs the same conversion.
}
\examples{
extent_to_bbox(c(100, 160, -60, -20))
bbox_to_extent(c(100, -60, 160, -20))
}
