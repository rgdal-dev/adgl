% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-to.R
\name{write_to}
\alias{write_to}
\title{Write a plan to a file}
\usage{
write_to(x, dsn, ...)
}
\arguments{
\item{x}{A source from \code{\link[=src]{src()}}, usually after \code{\link[=query]{query()}}.}

\item{dsn}{Where to write it.}

\item{...}{The named arguments below.}
}
\value{
\code{dsn}, invisibly.
}
\description{
\code{write_to()} is the terminal verb that sends a plan to any format in GDAL's
suite rather than into R. The driver comes from the file extension unless
you name one.
}
\details{
A warped plan compiles straight into GDAL's own reprojection pipeline and
never enters R at all, which is what you want for a large result. Every
other plan is read into memory and written back out, because the read and
the write are each one call and the intermediate is the size of the result
you asked for rather than the size of the source. If that intermediate is
too big, narrow the plan with \code{dim} first, or tile it yourself.
}
\section{Arguments}{


\describe{
\item{\code{driver}}{The GDAL driver's short name. The default reads it from
the extension of \code{dsn}.}
\item{\code{options}}{Character vector of \code{"KEY=VALUE"} creation options,
such as \code{c("COMPRESS=ZSTD", "TILED=YES")}.}
\item{\code{layer}}{Vector only. The layer name to create. Defaults to the
file's base name.}
\item{\code{geometry_name}, \code{fid_name}}{Vector only. What the written layer
calls its geometry and its feature id. \code{NULL}, the default, leaves it
to the driver, which is \code{geom} and \code{fid} for a GeoPackage. Only a format
that stores them as named columns can take a name (GeoPackage, SQLite,
PostgreSQL, Parquet); a shapefile or GeoJSON has nowhere to put one,
and GDAL warns and ignores it.}
}
}

\examples{
x <- src(system.file("extdata/test.tif", package = "GDAL7"))
path <- tempfile(fileext = ".tif")
write_to(query(x, dim = c(10, 5)), path)
src(path)
unlink(path)
}
