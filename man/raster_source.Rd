% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/src.R
\name{raster_source}
\alias{raster_source}
\alias{vector_source}
\title{The source classes}
\usage{
raster_source(
  dsn = character(0),
  dataset = NULL,
  info = list(),
  plan = list(),
  findings = NULL
)

vector_source(
  dsn = character(0),
  dataset = NULL,
  info = list(),
  plan = list(),
  findings = NULL
)
}
\arguments{
\item{dsn}{The GDAL connection string the source was opened from.}

\item{dataset}{The open \code{GDAL7::GDALDataset}.}

\item{info}{What the one probe at open returned: \code{GDAL7::gdal_info()} for a
raster, the layer's own description for a vector.}

\item{plan}{The pending query, as a plain list. Nothing in it is a GDAL
object, which is what lets it print, compare and serialise.}

\item{findings}{The deficiency report, computed once at open. Read it with
\code{\link[=report]{report()}}.}
}
\value{
A source object.
}
\description{
The two classes \code{\link[=src]{src()}} returns. They are exported so that another package
can write methods for them; build one with \code{\link[=src]{src()}} rather than by calling
these directly.
}
\examples{
S7::S7_inherits(src(system.file("extdata/test.tif", package = "GDAL7")),
                raster_source)
}
