% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxy.R
\name{as_grd}
\alias{as_grd}
\title{A raster source as a lazy grid}
\usage{
as_grd(x)
}
\arguments{
\item{x}{A raster source from \code{\link[=src]{src()}}, usually after \code{\link[=query]{query()}}.}
}
\value{
A \code{wk_grd_rct} whose \code{data} reads on demand.
}
\description{
\code{as_grd()} returns a \code{\link[wk:grd]{wk::grd_rct()}} whose data is a proxy rather than an
array. Nothing is read until something subsets it, and every subset becomes
one windowed GDAL read at exactly the size asked for, which GDAL serves
from whichever overview fits.
}
\details{
That is what makes \code{plot()} cheap. \code{wk}'s own \code{plot()} method reads the
device, works out a step, and crops the grid to it, so a 30000 by 30000
source drawn into an 800 by 600 device reads about 800 by 600 pixels.
\code{grd_crop()}, \code{grd_extend()}, \code{grd_tile()} and \code{grd_subset()} all work the
same way.

Two details are worth knowing. wk censors out-of-range indices to \code{NA}, so
a request that runs off the edge of the raster comes back padded with \code{NA}
rather than short. And \code{[} is a general interface: when the indices are an
arithmetic sequence, which is all \code{grd_crop()} ever produces, the read is
one \code{RasterIO} call at the output size; when they are not, the proxy reads
the bounding window at full resolution and subsets in R, and says so when
that window is large.
}
\examples{
x <- src(system.file("extdata/test.tif", package = "GDAL7"))
g <- as_grd(x)
dim(g$data)

# Reading a corner is one windowed read, not a read of the whole raster.
dim(wk::grd_subset(g, i = 1:2, j = 1:3)$data)
}
