% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{adgl-plot}
\alias{adgl-plot}
\title{Draw a source}
\arguments{
\item{x}{A source from \code{\link[=src]{src()}}, usually after \code{\link[=query]{query()}}.}

\item{y}{Unused, and present only because \code{plot()}'s own signature has it.}

\item{...}{Passed to the underlying plotting method, plus \code{as} for a
raster: \code{"grd"} (the default) draws through \code{\link[wk:grd]{wk::grd_rct()}}, and \code{"gis"}
reads at about the device's resolution and draws through
\code{\link[ximage:ximage]{ximage::ximage()}}, which handles the three- and four-band cases as RGB.}
}
\value{
The drawn object, invisibly.
}
\description{
\code{plot()} is the third terminal verb, and it is lazy in the way its kind of
data allows.
}
\details{
For a raster that means \emph{resolution} laziness: the plan becomes a lazy grid
(\code{\link[=as_grd]{as_grd()}}) and \code{wk}'s own grid plotting reads the device, works out a
step, and asks for about as many pixels as the device has, which GDAL
serves from whichever overview fits. Drawing a 30000 by 30000 source costs
the bytes of an 800 by 600 read, not of the file.

For a vector it means \emph{extent} laziness only, and the difference is worth
being plain about. There is no geometry overview and building one would be
exactly the automatic virtualisation this package refuses, so a wide view
of a big layer reads a lot of features. When the layer says how many that
is and the number is large, \code{plot()} says so before it starts.
}
\examples{
x <- src(system.file("extdata/test.tif", package = "GDAL7"))
plot(query(x, bands = 1))

v <- src(system.file("extdata/test.gpkg", package = "GDAL7"))
plot(v)
}
