% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{collect}
\alias{collect}
\title{Read a plan into memory}
\usage{
collect(x, ...)
}
\arguments{
\item{x}{A source from \code{\link[=src]{src()}}, usually after \code{\link[=query]{query()}}.}

\item{...}{The named arguments below.}
}
\value{
For a raster, a \code{wk_grd_rct} or a vector carrying a \code{"gis"}
attribute. For a vector, a tibble.
}
\description{
\code{collect()} is the terminal verb that turns a plan into R data. Everything
before it is free; this is where the reading happens, in one GDAL call for
a raster and one Arrow stream for a vector.
}
\details{
For a raster, \code{as} picks the in-memory form:
\itemize{
\item \code{"grd"}, the default, is \code{\link[wk:grd]{wk::grd_rct()}}: an array with
\code{dim = c(ny, nx, nbands)}, y decreasing down the rows, in a bounding
rectangle that carries the CRS. It costs one transpose per band, because
GDAL's x-fastest order is the transpose of what R's \code{matrix()} builds,
and it buys \code{plot()}, \code{grd_crop()}, \code{grd_tile()} and the rest of wk's
grid vocabulary.
\item \code{"gis"} is the shape \code{gdalraster::read_ds()} returns: one flat atomic
vector, band after band, x varying fastest within a band, carrying a
\code{"gis"} attribute of \code{type}, \code{bbox}, \code{dim}, \code{srs} and \code{datatype}. It is
the zero-transpose path, and \code{\link[ximage:ximage]{ximage::ximage()}} draws it directly.
}

\code{type} is the R type the values are read into. \code{"raw"} is one byte a value
and is what an RGB image wants; \code{"integer"} is four and covers Byte, Int8,
Int16, UInt16 and Int32. A band whose type will not fit is an error rather
than a silent clamp. The \code{"grd"} form is built from whatever comes back, so
a raw read stays a raw array.

\code{mask} decides what happens to a value the source itself declares absent.
A band's nodata value is a fill, not a measurement, so by default it comes
back as \code{NA} rather than as the number that stands for it. This is what
terra and stars do, and what rasterio calls \code{masked}. It pivots on \code{type},
because only a double has a missing value to put there: \code{mask} defaults to
\code{TRUE} for \code{"double"} and to \code{FALSE} for \code{"integer"} and \code{"raw"}, and
asking for it with either of those is an error rather than a pretence.

Masking is exact rather than approximate, and that matters when the read is
resampled. A nearest read returns source values unchanged, so every fill
pixel is caught. An averaging or bilinear read blends a fill with its
neighbours, and the blend is no longer equal to the nodata value, so it
survives as a number. That is the same in every package that does this, and
it is why \code{resample = "nearest"} is the default.

For a vector, the result is a tibble whose geometry column is \code{\link[wk:wkb]{wk::wkb()}}
with its CRS set. The id is always \code{fid} and the geometry always \code{geom},
whatever the driver called them. An attribute that already has one of
those names comes back as \code{fid_1} or \code{geom_1}. When the plan carries a \code{crs} from
\code{\link[=query]{query()}}, the coordinates are transformed with \code{\link[PROJ:proj_trans]{PROJ::proj_trans()}} on
the way out, which is one pass over the geometry and nothing else.

The whole vector plan is carried out in GDAL: the filters, the fields,
which the driver is told not to read at all, the names, and the limit,
which stops the read rather than trimming it. So \code{as = "arrow"} can hand
back the Arrow stream itself, unread, as a \code{nanoarrow_array_stream} for
arrow, duckdb, geoarrow or anything else that takes one, with the CRS in
the geometry column's GeoArrow metadata. The one part of a plan that is
not in GDAL is \code{query(crs = )}, so a stream of a reprojected plan is an
error rather than a stream in the wrong CRS. A layer allows one stream at a
time: read it through, or release it with \code{\link[GDAL7:release_arrow_stream]{GDAL7::release_arrow_stream()}},
before reading the source again.
}
\section{Arguments}{


\describe{
\item{\code{as}}{For a raster, \code{"grd"} (the default) or \code{"gis"}. For a
vector, \code{"tibble"} (the default) or \code{"arrow"}.}
\item{\code{type}}{Raster only. \code{"double"} (the default), \code{"integer"} or
\code{"raw"}.}
\item{\code{mask}}{Raster only. Return each band's nodata value as \code{NA}.
Defaults to \code{TRUE} for \code{type = "double"} and \code{FALSE} otherwise.}
}
}

\examples{
x <- src(system.file("extdata/test.tif", package = "GDAL7"))
g <- collect(query(x, dim = c(10, 5)))
dim(g$data)

v <- src(system.file("extdata/test.gpkg", package = "GDAL7"))
collect(query(v, where = "population > 1e6"))
}
