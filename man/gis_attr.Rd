% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{gis_attr}
\alias{gis_attr}
\title{The gis attribute}
\usage{
gis_attr(bbox, dim, srs, datatype)
}
\arguments{
\item{bbox}{\code{c(xmin, ymin, xmax, ymax)}.}

\item{dim}{\code{c(nx, ny, nbands)}.}

\item{srs}{The CRS as WKT, or \code{""}.}

\item{datatype}{Character, the GDAL type name of each band.}
}
\value{
A list of five elements: \code{type}, \code{bbox}, \code{dim}, \code{srs}, \code{datatype}.
}
\description{
\code{gis_attr()} builds the attribute that \code{gdalraster::read_ds()} puts on its
result, which \code{\link[ximage:ximage]{ximage::ximage()}} reads to draw an image in its own
coordinates. \code{adgl} emits this shape without importing gdalraster, so the
contract is reproduced here and asserted by a test rather than inherited.
}
\details{
Read from gdalraster at its master branch on 2026-09-20. Note the two
orders: \code{bbox} is \code{c(xmin, ymin, xmax, ymax)} while \code{dim} is
\code{c(nx, ny, nbands)}, with x first.
}
\examples{
gis_attr(c(0, 0, 10, 10), c(10L, 10L, 1L), "", "Byte")
}
