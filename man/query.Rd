% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{query}
\alias{query}
\title{Narrow a source}
\usage{
query(x, ...)
}
\arguments{
\item{x}{A source from \code{\link[=src]{src()}}.}

\item{...}{The named arguments below.}
}
\value{
\code{x} with the plan narrowed.
}
\description{
\code{query()} adds to a source's plan and reads nothing. It composes, so a
pipeline of \code{query()} calls is the same as one call with all the arguments,
and the result is still a source you can print, query again, or hand to a
terminal verb.
}
\details{
Some arguments mean the same thing for both kinds of source and some do
not, which is deliberate. \code{extent} is the same operation on both: restrict
to this rectangle. \code{bands} and \code{fields} are the same operation on different
nouns. But resolution has no vector meaning and attribute filtering has no
raster meaning, so \code{dim} and \code{resample} are raster-only and \code{where} and
\code{limit} are vector-only. Passing one to the wrong kind is an error rather
than a silent no-op, because a spatial query that quietly ignored half of
what you asked for is worse than one that stops.

\code{crs} is the one argument that means something different for each kind, and
the difference is the point rather than an inconsistency. For both, an
\code{extent} given beside it is read in that CRS and transformed onto the
source to pick what to read, which is four numbers moving and no data
touched. For a vector it additionally sets the CRS the features come back
in, because transforming coordinates is cheap, local and has no resampling
decision in it. For a raster it does not, because reprojecting a grid
resamples: that is \code{\link[=warp]{warp()}}, a separate verb so the method stays a choice
you make rather than one you inherit.

A raster \code{extent} snaps outward to whole source pixels, so a query with no
\code{dim} reads the source's own values rather than a resampling of them, and
the extent the result carries is the one its pixels really have.
}
\section{Arguments}{


All of these are passed by name.

\describe{
\item{\code{extent}}{\code{c(xmin, xmax, ymin, ymax)}, the rectangle to restrict
to.}
\item{\code{crs}}{Anything GDAL and PROJ understand. An \code{extent} beside it
is read in this CRS. For a vector source it is also the CRS the
features come back in; for a raster it is not, and \code{\link[=warp]{warp()}} is.}
\item{\code{bands}}{Raster only. Which bands, one-based, or by band
description.}
\item{\code{dim}}{Raster only. \code{c(nx, ny)}, the size to read the window at.
This is what makes a read cheap: asking for a small \code{dim} over a large
extent lets GDAL serve it from an overview.}
\item{\code{resample}}{Raster only. One of \code{"nearest"} (the default),
\code{"bilinear"}, \code{"cubic"}, \code{"cubicspline"}, \code{"lanczos"}, \code{"average"},
\code{"mode"}, \code{"gauss"} or \code{"rms"}.}
\item{\code{where}}{Vector only. An SQL \code{WHERE} clause. GDAL does not
validate it when it is set, so a bad clause shows up as a warning at
read time rather than an error here.}
\item{\code{fields}}{Vector only. Which attribute columns to read, by name.}
\item{\code{limit}}{Vector only. Stop after this many features.}
}
}

\examples{
x <- src(system.file("extdata/test.tif", package = "GDAL7"))
query(x, extent = c(-100, 0, 0, 80), dim = c(8, 8), resample = "average")

v <- src(system.file("extdata/test.gpkg", package = "GDAL7"))
query(v, where = "population > 1e6", fields = "name")
}
