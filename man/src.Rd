% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/src.R
\name{src}
\alias{src}
\title{A lazy spatial source}
\usage{
src(dsn, options = NULL, drivers = NULL, layer = 1L)
}
\arguments{
\item{dsn}{A GDAL connection string: a path, or a \verb{/vsicurl/...},
\verb{/vsis3/...}, \code{GPKG:...} or any other form GDAL understands.}

\item{options}{Character vector of \code{"KEY=VALUE"} open options, or \code{NULL}
for none.}

\item{drivers}{Character vector of driver short names that may open the
source, or \code{NULL} to place no restriction.}

\item{layer}{For a vector source, the layer to plan over: a name, or a
one-based position. Ignored for a raster.}
}
\value{
A \code{raster_source} or a \code{vector_source}.
}
\description{
\code{src()} opens a GDAL data source and returns a plan over it, never data.
The source is probed once at open, so the object knows its own shape and
what its metadata is missing, and nothing is read until \code{\link[=collect]{collect()}},
\code{\link[=write_to]{write_to()}} or \code{plot()} asks for a result.
}
\details{
\code{options} is the non-virtualisation way to fix a deficient source, which is
why it is here and why \code{\link[=report]{report()}} points at it: a CSV's
\code{"X_POSSIBLE_NAMES=lon"}, a raster's \code{"OVERVIEW_LEVEL=2"}. It is handed
straight to GDAL. \code{drivers} restricts which drivers may try the file, so a
second driver cannot claim one the first should have had.
}
\examples{
x <- src(system.file("extdata/test.tif", package = "GDAL7"))
x

v <- src(system.file("extdata/test.gpkg", package = "GDAL7"))
v
}
